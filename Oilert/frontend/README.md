# To-Do List Application Frontend

Overview of the frontend setup for a To-Do List application. The frontend is built using React.  Providing a basic GUI for CRUD operations for managing tasks.

## Technology Stack
Runtime Environment: Node.js
Framework: React

## Setup and Installation
Before running the frontend, ensure you have Node.js installed. Then, install the required packages using npm.

```
npm i
npm run start
```
